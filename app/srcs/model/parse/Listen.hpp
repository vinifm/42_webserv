#ifndef LISTEN_HPP
# define LISTEN_HPP

#include <iostream>
#include <vector>

class Listen
{
public:
	Listen();
	~Listen();

	void	initListen(const std::stringstream& values);

private:
	// std::string	_address;
	// int			_port;

};

#endif

